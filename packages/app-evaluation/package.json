{
  "name": "@pickle-pee/evaluation",
  "version": "1.0.4",
  "description": "Smoke scenarios, protocol verification, regression baselines, and subagent acceptance tests",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "node -e \"const fs = require('node:fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.rmSync('tsconfig.build.tsbuildinfo', { force: true });\" && tsc -p tsconfig.build.json",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pickle-pee/runtime": "1.0.4",
    "@pickle-pee/tools": "1.0.4"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
