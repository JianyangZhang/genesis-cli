{
	"name": "@anthropic-ai/claude-code-pi-runtime",
	"version": "0.0.0",
	"private": true,
	"description": "Product-layer runtime: session facade, plan execution, event normalization",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"test": "vitest run"
	}
}
